				
Must Have:
- as well safe wrong stuff to file ( possibility to safe invalid files )

- bug: application-arguments: verbose gleichzeitig mit pass filename 

- rename packages: crontab-gui + config-gui-base
- display filename

- validate wizard-value when typed
- validate whole file when wizard finished and after load / tab-switch
- check that string-size is not exceeded ( use expandString as much as possible ) 
- use automake

- separate config-gui-base from cron-gui
- add examples for lightdm, fstab
- Tooltips
- fstab-gui, lightdm-gui
- Multi Language

Planned Features:
- build patch for step-size stuff in crontab: https://bugs.debian.org/cgi-bin/bugreport.cgi?bug=830821

- colored editor
- copy / paste in menu

- separate gui-components -re-use for anacron, fstab, lightdm, rsnapshot, ...
- Wizard-GUI's: Möglichkeit Interval direkt zu tippen ( validieren + aktives Fenster Zeigt getippten Interval )
- Menü --> Datein --> Drucken
- Menü --> Bearbeiten --> Rückgängig / Widerholen , Löschen, Einstellungen
- Menü --> Ansicht Farbschema